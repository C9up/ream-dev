[package]
name = "inker-engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "inker_engine"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
once_cell = "1"
regex = "1"
