node_modules/
target/
# …including when it is a symlink pointing the build output elsewhere,
# which `target/` does not match.
target
dist/
*.node
.env
.env.*
.env-*
.DS_Store
.turbo/
coverage/
*.log

# Separate standalone project — lives on disk, never tracked in ream
apps/syndic/

# Local-only working files (not part of the published source tree)
.review/
BMAD_TRACEABILITY.md
packages/IMPROVEMENTS_95_PLUS.md
packages/*/CORRECTIONS.md

# BMAD spike scratch (throwaway, never committed)
spikes/

# ream-mcp local index
.ream-mcp/
