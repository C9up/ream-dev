{
  "name": "ream-dev",
  "version": "0.0.0",
  "private": true,
  "description": "Ream development workspace",
  "scripts": {
    "test": "pnpm -r --workspace-concurrency=1 run test",
    "test:flat": "vitest run",
    "test:rust": "cargo test --all",
    "build": "pnpm -r --filter './packages/*' run build",
    "build:rust": "cargo build --release",
    "lint": "pnpm -r --workspace-concurrency=1 run lint",
    "quality:duplicates": "bash scripts/duplicate-report.sh",
    "quality:gates": "node scripts/check-publish-gates.mjs",
    "quality:rust": "bash scripts/quality-rust.sh",
    "quality:duplicates:strict": "bash scripts/duplicate-report.sh --strict",
    "quality:duplicates:actionable": "bash scripts/duplicate-report.sh --actionable",
    "quality:packages": "bash scripts/quality-packages.sh",
    "quality:peer-ranges": "node scripts/check-peer-ranges.mjs",
    "quality:service-accessors": "node scripts/check-service-accessors.mjs",
    "vendor:sync": "node scripts/vendor-sync.mjs",
    "vendor:check": "node scripts/vendor-sync.mjs --check",
    "release:notes": "node scripts/release-notes.mjs",
    "quality:all": "pnpm quality:duplicates && pnpm quality:packages && pnpm quality:peer-ranges && pnpm quality:service-accessors && pnpm vendor:check",
    "verify:all": "bash scripts/verify-all.sh",
    "dev": "tsx watch apps/demo/server.ts",
    "start": "tsx apps/demo/server.ts",
    "bench": "tsx benchmarks/bench.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@types/node": "^22.20.1",
    "@vitest/coverage-v8": "4.1.11",
    "typescript": "^6.0.3",
    "vitest": "4.1.11",
    "yaml": "^2.9.0"
  },
  "//engines": "Node 24, not 22, because AsyncLocalStorage sits on the request hot path — the container resolves through it, HttpContext is it, and the emitter reads it on every event. Node 24 backs it with AsyncContextFrame by default, and measured on that exact pattern (one context per request, six async boundaries, several reads) it costs 0.61 us per request instead of 1.55 us: 2.5x, on every request, for free. Before 24 the same mechanism existed only behind an experimental flag, and a framework cannot base its performance on a flag the application has to remember to pass. This is NOT 'track the current LTS' — security fixes still land in 22 until 2027, npm 11 is irrelevant under pnpm, and node:sqlite is not what atlas uses.",
  "engines": {
    "node": ">=24.0.0"
  }
}
